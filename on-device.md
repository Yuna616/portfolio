# 온디바이스 시험 부정행위 감지 시스템

> 웹캠 영상 하나로 시험 부정행위를 탐지하는 **완전 온디바이스 엣지 AI 시스템**.
> 비전 파이프라인과 LLM 판정을 Qualcomm Hexagon NPU 2개 코어에 분리 배치해,
> 클라우드 의존 없이 보드 단독으로 실시간 동작합니다.

<!-- [TODO] 여기에 데모 GIF 또는 웹 UI 스크린샷 (본인 얼굴로 새로 촬영 권장) -->
<!-- [TODO] 보드 실물 사진 (Radxa Airbox Q900) -->

| | |
|---|---|
| **기간** | [ 2026.06 – present] |
| **팀 구성** | 4인 팀 프로젝트 |
| **담당 역할** | 모델 평가·선정, 비전 모델 NPU 배포, ByteTrack Rust 포팅, 카메라 파이프라인, 시선 캘리브레이션, 관제 서버(Rust) |
| **하드웨어** | Radxa Airbox Q900 — Qualcomm QCS9075, Hexagon NPU 2코어 |
| **기술 스택** | Rust (Axum, ort/ONNX Runtime QNN EP, GStreamer), Python (평가 하네스), Qualcomm QAIRT, 모델 양자화 (w8a16 / w8a8 / FP16), Linux aarch64 |

---

## 시스템 아키텍처

```
[USB 웹캠 /dev/video0]
        │  V4L2 캡처 — DeviceMonitor로 카메라 장치·캡처 모드 자동 감지
        ▼
[GStreamer 파이프라인] ─ H.264 VPU 하드웨어 디코드 (미지원 환경은 SW 디코드 fallback)
        │
        ▼
┌─ Hexagon NPU core 0 ── 비전 파이프라인 (Rust + ONNX Runtime QNN EP) ──┐
│  face_det_lite(w8a16) → facemap_3dmm(w8a8) → PureGaze(시선 추정)      │
│  RF-DETR Nano(FP16, 사람 검출) → ByteTrack(순수 Rust 다중 인원 추적)   │
│  * CPU fallback 차단 — 전 구간 NPU 실행 보장                          │
└──────────────┬────────────────────────────────────────────────────────┘
               │  시선 캘리브레이션 (50프레임 baseline · MAD 이상치 제거
               │                     · 절대각 게이트 · EMA 안정화)
               │  → 인원별 시선/행동 특징 집계 (JSON)
               ▼
┌─ Hexagon NPU core 1 ── LLM 판정 ──────────────────────────────────────┐
│  Qwen3-4B (w4a16 양자화), QAIRT Genie 인프로세스 실행                  │
└──────────────┬────────────────────────────────────────────────────────┘
               ▼
[관제 서버 (Rust/Axum) :8080]
   · 프레임 long-poll 스트리밍  · 감지 레코드 ring buffer(1.8만 건)
   · 판정 결과 REST API        · 세션별 녹화 재생
               ▼
[브라우저 — 실시간 모니터링 UI]
```

핵심 설계는 **NPU 코어 분리**입니다. 비전 추론(core 0)과 LLM 판정(core 1)을
서로 다른 코어에 고정 배치해, 초당 수십 프레임의 비전 추론이 돌아가는 동안에도
LLM 판정이 이를 블로킹하지 않습니다.

---

## 내가 맡은 부분

- **모델 평가·선정** — 시선 추정 모델 벤치마크, LLM 판정 정확도 하네스
- **비전 모델 NPU 배포** — 4종 모델을 ONNX Runtime QNN EP로 Hexagon NPU에 올림
- **ByteTrack 포팅** — Python 원본을 순수 Rust로 재구현
- **카메라 파이프라인** — GStreamer V4L2 캡처 + VPU 하드웨어 디코드
- **시선 캘리브레이션** — 세션별 baseline·게이트·안정화 로직 설계와 튜닝
- **관제 서버** — Rust/Axum 기반 REST 허브

(LLM 온디바이스 실행(Genie 연동) 등 나머지는 팀원 담당)

---

## 문제 해결 스토리 1 — 벤치마크 1등 모델이 현장에서 낸 회귀

**문제.** 기존 시선 추정 모델(L2CS-Net)의 도메인 일반화 성능이 부족해 교체가 필요했습니다.

**접근.** 후보 모델들을 MPIIFaceGaze(45k 샘플, 15명 피험자) 크로스 데이터셋
MAE(Mean Angular Error)로 벤치마크했습니다.

| 모델 | MAE (°) |
|---|---|
| **PureGaze (ResNet50)** | **7.10** |
| yakhyo gaze-estimation (ResNet34) | 7.36 |
| L2CS-Net (기존) | 8.07 |
| GazeTR-Hybrid | 8.69 |

PureGaze를 선정해 교체했는데 — **실제 평가 클립에서 오탐(FP)이 6건으로 회귀**했습니다.
벤치마크 수치가 가장 좋은 모델도 현장 조건(카메라 축에서 벗어난 절대각)에서는
기존 튜닝값과 맞지 않았던 것입니다.

**해결.** 원인을 추적해 캘리브레이션 체계를 재설계했습니다.

- 세션 시작 시 **50프레임 baseline** 수집 (30/70/100프레임 스윕으로 50 결정)
- 수집 구간의 **MAD(중앙값 절대 편차) 8° 초과 시 구간 연장** — 오염된 baseline 방지
- **절대각 게이트** (yaw 39° / pitch 30°) — baseline과 무관한 극단적 시선 이탈 차단
- **EMA 안정화** (α=0.4) + 3프레임 dwell + 3° 히스테리시스 — 프레임 단위 떨림 억제

**결과.** 동일 평가셋에서 **오탐 6건 → 2건, 정탐 10건 전량 유지 (recall 1.0)**.
모든 파라미터는 환경변수로 노출해 재튜닝 없이 현장 조정이 가능하게 했습니다.

---



---

## 카메라 파이프라인과 관제 서버

- **GStreamer 캡처 파이프라인**: V4L2 캡처, H.264는 보드의 VPU로 하드웨어 디코드
  (`v4l2h264dec`), 미지원 환경은 `decodebin` SW 경로로 자동 fallback.
  DeviceMonitor로 `/dev/videoN` 장치와 지원 캡처 모드를 자동 감지해
  카메라가 바뀌어도 설정 수정이 필요 없습니다.
- **관제 서버 (Rust/Axum)**: 파이프라인이 프레임·감지 결과·판정을 POST하고
  브라우저가 조회하는 허브. 프레임은 latest-wins(손실 허용)로 가볍게,
  감지 레코드는 ring buffer(기본 1.8만 건)로 신뢰성 있게 — 데이터 성격에 따라
  전달 보장을 다르게 설계했습니다. 세션별 녹화 재생도 제공합니다.

---

## 검증 방법

- **평가 픽스처**: 부정행위 클립 8종 + 정상 케이스 2종, ground truth 라벨 관리
- **정확도 하네스**: confusion matrix(TP/FP/FN/TN) 기반 recall·precision·specificity 산출
- **프롬프트 스키마 A/B**: full 스키마 recall 1.0 확인, compact 스키마는
  판정 지연을 절반으로 줄이는 대신 specificity가 하락 — 트레이드오프를 정량화해 선택
- **반복 안정성**: 동일 입력 반복 실행으로 LLM 판정의 완전 결정성(temp=0) 검증

---

## 회고

- **정상 케이스 부족**: 평가셋의 정상 케이스가 2건뿐이라 specificity 검증이
  약했습니다. 오탐 지표의 신뢰도를 높이려면 정상 시나리오 촬영을 늘려야 합니다.
- **벤치마크 ≠ 현장 성능**: 크로스 데이터셋 MAE가 가장 좋은 모델도 실제 카메라
  배치 조건에서 회귀를 냈습니다. 모델 교체 시 다운스트림 파이프라인 전체의
  엔드투엔드 재평가가 필수라는 것을 체득했습니다.
- [TODO: 개인적으로 배운 점 1~2줄 — 예: Rust로 임베디드 파이프라인을 처음 구축하며 느낀 점]

<!-- [TODO] 공개 가능 여부 확인 후: 리포 링크 / 시연 영상 링크 -->
